import PolicyContent from '@/components/PolicyContent';

const BiddingStatus = () => {
  return (
    <PolicyContent endpoint="bidding-status" title="Bidding Status" />
  );
};

export default BiddingStatus;