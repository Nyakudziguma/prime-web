import PolicyContent from '@/components/PolicyContent';

const DepositBalance = () => {
  return (
    <PolicyContent endpoint="deposit-balance" title="Deposit Balance" />
  );
};

export default DepositBalance;